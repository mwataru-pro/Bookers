<h1>Editing Book</h1>
<%= form_for(@book, url:"/books/#{@book.id}") do |f| %>

<h4>Title</h4>
<%= f.text_field :title %>

<h4>本分</h4>
<%= f.text_area :body %>

<br><%= f.submit 'Update Book' %>

<% end %>

<p>
 <%= link_to "show",book_path %>
 |
 <a href="/books">Back</a>
</p>